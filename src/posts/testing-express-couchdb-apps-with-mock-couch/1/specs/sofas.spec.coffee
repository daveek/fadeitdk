should = require 'should'

describe 'Sofas API', ->

  describe '/sofas', ->
    it 'should return all sofas with GET', ->
      # TODO: write test
      (false).should.be.true()

    it 'should create a new sofa with POST', ->
      # TODO: write test
      (false).should.be.true()

  describe '/sofas/:sofa', ->
    it 'should return a particular sofa with GET', ->
      # TODO: write test
      (false).should.be.true()

    it 'should update a particular sofa with PUT', ->
      # TODO: write test
      (false).should.be.true()

    it 'should delete a particular sofa with DELETE', ->
      # TODO: write test
      (false).should.be.true()
